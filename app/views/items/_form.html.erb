<fieldset>
  <%= hidden_field_tag :box_id, params[:id] %>
  <div class="clearfix">
    <%= f.label :title %>
    <div class="input">
      <%= f.text_field :title %>
    </div>
  </div>
  <div class="clearfix">
    <%= f.label :description %>
    <div class="input"><%= f.text_area :description, :rows => 3 %></div>
  </div>
  <div class="clearfix">
    <%= f.label :tags %>
    <div class="input">
      <%= f.text_field :tags, :value => @item.tags_to_s %>
    </div>
  </div>
  <% @template.global_fields.each do |gf| %>
    <div class="clearfix">
      <label for="<%= "item_#{gf}" %>"><%= gf.humanize %></label>
      <div class="input"><%= text_field_tag "item[#{gf}]" %></div>
    </div>
  <% end %>
  <% @template.user_fields.each do |uf| %>
    <div class="clearfix">
      <label for="<%= "user_item_#{uf}" %>"><%= uf.humanize %></label>
      <div class="input"><%= text_field_tag "user_item[#{uf}]", (eval("@item.#{uf}") unless @item.new?) %></div>
    </div>
  <% end %>
  <div class="clearfix">
    <div class="input">
      <%= f.label :image %>
      <%= f.file_field :image %>
    </div>
  </div>
  
  <div class="actions">
    <%= f.submit 'Submit', :class => 'btn primary' %>
    <%= link_to 'Cancel', items_path, :class => 'btn' %>
  </div>
</fieldset>
