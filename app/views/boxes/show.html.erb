<h2><%= @box.name %></h2>

<p>Category : <%= @box.category.name unless @box.category.nil? %></p>

<% unless @box_items.blank? %>
  <h3>Items</h3>
  <% for item in @box_items %>
    <ul>
      <li><%= link_to item.title, item_path(item) %></li>
    </ul>
  <% end %>
<% end %>

<p><%= link_to "Add an Item", new_item_path(:id => @box.id) %></p>
